#!/bin/sh
# .git/hooks/prepare-commit-msg
# Example hook to generate commit message using Copilot commit skill
# Usage: Place this script as .git/hooks/prepare-commit-msg and make it executable

COMMIT_MSG_FILE=$1

# Get staged changes summary
git diff --cached --name-only > /tmp/commit_changes.txt
CHANGES=$(cat /tmp/commit_changes.txt)

# Call Copilot/ChatGPT or your CLI tool here to generate commit message
# Example placeholder (replace with actual CLI call):
# copilot-commit-skill --input "$CHANGES" > $COMMIT_MSG_FILE

echo "# TODO: Integrate with Copilot/ChatGPT commit message skill" >> $COMMIT_MSG_FILE
